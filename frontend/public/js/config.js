
export const config = {
    isDevelopment: true,
    backendUrl: 'https://gameconnecting-backend.vercel.app',
    socketUrl: 'wss://gameconnecting-backend.vercel.app',
    apiKey: 'FlowerRealmGameConnecting',
    frontendUrl: 'http://localhost:3000',
    maxRetryAttempts: 3,
    reconnectionDelay: 1000,
    connectionTimeout: 5000,
    heartbeatInterval: 30000
};
